[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --strict-config --strict-markers --cov=src
markers =
    unit: unit tests
    integration: integration tests (requires --run-integration)
    e2e: end-to-end tests (requires --run-e2e)
